import { Navigate } from 'react-router-dom';

/** Module Évaluation classe — même application que Jules Verne ; droits selon abonnement CI / JV / candidat FR. */
export function EvalClasseCI() {
  return <Navigate to="/apps/eval-classe" replace />;
}
